# 2026-09: PNG/JPEG desktop wallpaper (stb_image)

- Wallpaper paths may be `.bmp`, `.png`, `.jpg`, or `.jpeg` (case-insensitive).
- Dependency choice: fetch pinned `stb_image.h` at build time (`third_party/stb/`); keep BMP on `SDL_LoadBMP`. See `third_party/stb/README.md`.
- `WallpaperImage` loads PNG/JPEG; Window Manager cover-scale behavior unchanged.
- Settings Appearance hint and Tab completion accept the new extensions; sample PNG seeded at `/Wallpapers/sample.png`.
- Slice 7.1 done; next is 7.2 (Fourth game).
