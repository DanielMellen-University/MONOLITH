# Shell polish

- Rebuild Start menu hit targets from `AppRegistry` (`wm_start_menu_rows.inc`).
- Retire the hardcoded `wm_start_menu_entries.inc` table so render and hit-testing share one menu.
- Update integration verifies for the unified Start menu path.
- Slice `shell-polish` done; next is `await-5.x-unpark` (Phase 5-6 remain parked).
