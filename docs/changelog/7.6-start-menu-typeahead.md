## 7.6 - Start menu type-ahead filter (2026-09-22)

- Filter Start menu rows by case-insensitive label substring while open.
- Wire TEXTINPUT / Backspace; Escape clears filter first, then closes.
- Show active filter in Start menu chrome; rebuild hit targets on edit.
- Add `test_start_menu_filter` and `verify_start_menu_filter.sh`.
