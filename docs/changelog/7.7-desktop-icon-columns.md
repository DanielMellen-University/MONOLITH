## 7.7 - Multi-column desktop icons (2026-09-24)

- Fill additional desktop-icon columns left-to-right when usable height is short.
- Keep omitting icons that cannot fit entirely (no taskbar overlap, no right-edge clip).
- Extend headless layout tests and static verify checks for multi-column placement.
